{
  "name": "mihari-js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "test": "vitest run",
    "lint": "lerna run lint",
    "publish": "lerna publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "husky": "^9.0.0",
    "lerna": "^8.1.0",
    "tsup": "^8.5.1",
    "typescript": "^5.4.0",
    "vitest": "^3.1.1"
  }
}
